[package]
name = "exif"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["uspfe"]
license = "MIT"
description = "Read Exif metadata from images, through a WebAssembly plugin."
repository = "https://github.com/uspfe/typst-exif"
keywords = ["exif", "metadata", "image", "photo", "camera"]
categories = ["utility"]
compiler = "0.15.0"
exclude = [
  ".github",
  "Makefile",
  "examples",
  "plugin",
  "tests",
]
